import Link from "next/link";import { notFound,redirect } from "next/navigation";import { ArrowLeft } from "lucide-react";import { createClient } from "@/lib/supabase/server";import { saveMember } from "../actions";import { MemberForm } from "../member-form";
export default async function EditMember({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string;success?:string}>}){const [{id},q]=await Promise.all([params,searchParams]),s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect("/login");const [{data:member},{data:areas},{data:categories}]=await Promise.all([s.from("members").select("*").eq("id",id).single(),s.from("areas").select("id,name").eq("active",true).order("name"),s.from("member_categories").select("id,name").eq("active",true).order("name")]);if(!member)notFound();return <main className="module-page"><div className="form-wrap"><Link className="back" href="/members"><ArrowLeft size={17}/> Direktori Ahli</Link><header className="module-head"><div><p className="eyebrow">PROFIL AHLI · {member.member_no}</p><h1>{member.full_name}</h1><span>Kemas kini maklumat peribadi, kawasan, kategori dan status ahli.</span></div></header>{q.error&&<div className="error">{q.error}</div>}{q.success&&<div className="success">{q.success}</div>}<section className="card"><MemberForm action={saveMember} member={member} areas={areas||[]} categories={categories||[]}/></section></div></main>}
